# Production Environment Variables for Vercel
# These settings optimize the app for serverless deployment

# Disable TensorFlow.js warnings in production
NEXT_PUBLIC_TF_BACKEND=webgl

# Node environment
NODE_ENV=production
